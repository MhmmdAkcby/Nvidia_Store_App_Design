import 'package:flutter/material.dart';

class ProjectPadding {
  static const paddingAllOne = EdgeInsets.all(8.0);
  static const paddingAllTwo = EdgeInsets.all(16.0);
  static const paddingAllThree = EdgeInsets.all(24.0);
  static const paddingOnlyTop20 = EdgeInsets.only(top: 70);
  static const padingOnlyTop10 = EdgeInsets.only(top: 10);
  static const edgeInsets = EdgeInsets.symmetric(vertical: 15, horizontal: 50);
  static const edgeInsets2 = EdgeInsets.symmetric(vertical: 10, horizontal: 110);
}
